apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mifosx-mariadb.fullname" . }}-test-connection"
  labels:
    {{- include "mifosx-mariadb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mifosx-mariadb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
