apiVersion: v1
kind: Service
metadata:
  name: {{ include "mifosx-mariadb.fullname" . }}
  labels:
    {{- include "mifosx-mariadb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mifosx-mariadb.selectorLabels" . | nindent 4 }}
